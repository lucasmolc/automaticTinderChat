# Automação de navegador
playwright==1.49.1

# Provedores de IA
openai>=1.59.0
tenacity>=9.0.0
requests>=2.32.3        # usado pelos providers Claude/DeepSeek

# Banco de dados
sqlalchemy==2.0.36
# SQLite é o padrão e já vem embutido no Python — nenhum driver extra necessário.
# Para outros bancos, instale o driver correspondente (veja docs/DATABASE.md):
#   SQL Server:  pip install pyodbc            (requer ODBC Driver instalado no SO)
#   PostgreSQL:  pip install psycopg2-binary

# Configuração
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.0

# Interface web
flask==3.1.0
flask-cors==5.0.0
flask-limiter>=3.9.0

# WebSocket (tempo real; degrada para polling se ausente)
flask-socketio>=5.4.1
python-socketio>=5.11.4
eventlet>=0.38.0

# Observabilidade (opcional)
prometheus-client>=0.21.1
prometheus-flask-exporter>=0.23.1

# A/B testing (hash determinístico de bucketing)
mmh3>=5.0.1

# Relatórios e análise de dados
pandas==2.2.3
matplotlib==3.9.3
seaborn==0.13.2
openpyxl==3.1.5

# Logging
loguru==0.7.3

# Testes
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
